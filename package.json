{
  "name": "@hecom/loginprocess",
  "version": "3.1.1",
  "description": "Login Process Module",
  "main": "src/index.js",
  "types": "./index.d.ts",
  "scripts": {
    "test": "jest --passWithNoTests",
    "major": "npm version major -m 'Bump version %s';git push origin master --tags",
    "patch": "npm version patch -m 'Bump version %s';git push origin master --tags",
    "minor": "npm version minor -m 'Bump version %s';git push origin master --tags"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hecom-rn/loginprocess.git"
  },
  "author": "Hecom",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@hecom/listener": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0"
  }
}
